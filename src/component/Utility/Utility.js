const add = (first, second) => {
    return first + second
};

//------single item export--------//
// export default add


//-------- malty line export ---------//

const malty = (first, second) => {
    return first * second
}

export { add, malty }