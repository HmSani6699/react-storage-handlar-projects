const add = (first, second) => {
    return first + second
};

//------single item export--------//
// export default add


//-------- malty line export ---------//

const multiply = (first, second) => {
    return first * second
}

export { add, multiply }